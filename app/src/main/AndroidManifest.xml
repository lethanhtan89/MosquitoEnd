<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mosquito.project.com.mosquitoend" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:theme="@style/MosquitoEnd" >

        <activity
            android:name=".SplashActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="mosquito.project.com.mosquitoend.MainActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
        </activity>

        <activity
            android:name="mosquito.project.com.mosquitoend.Mosquito1Activity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name="mosquito.project.com.mosquitoend.Mosquito2Activity"
            android:label="@string/app_name">
        </activity>

        <activity
            android:name="mosquito.project.com.mosquitoend.Mosquito3Activity"
            android:label="@string/app_name">
        </activity>
    </application>
</manifest>
